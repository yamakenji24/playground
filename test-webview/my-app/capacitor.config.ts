import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mynextwebapp',
  appName: 'my-next-webapp',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
